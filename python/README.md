# sensorsim (Python orchestrator)

Drives the C `sensorsim` binary across configurations, parses
JSON-line samples, and provides the harness that data-processing
test code targets instead of real hardware.
